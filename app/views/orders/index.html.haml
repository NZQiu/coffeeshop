%h1 All orders
%table
  %tr
    %th Id
    %th Item name
    %th Item cup size
    %th Item type
    %th Item price
  - @orders.each do |order|
    %tr
      %td= order.id
      %td= order.item.drink.name
      %td= order.item.cup_size
      %td= order.item.drink.drink_type
      %td= format_price order.item.price
%h3
  Total sales: #{format_price Order.total_sales}
= link_to "Add order", new_order_path
